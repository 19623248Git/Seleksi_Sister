#ifndef COMMON_TYPES_H
#define COMMON_TYPES_H

#include <cstdint>

// This is now the one and only official definition of Color for your project.
struct Color {
    uint8_t r, g, b;
};

#endif // COMMON_TYPES_H